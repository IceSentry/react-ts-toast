import styled from 'styled-components'

export const StyledProvider = styled.div`
  box-sizing: border-box;
  max-height: 100%;
  overflow: hidden;
  padding: 8px;
  position: fixed;
  bottom: 0;
  right: 0;
`
